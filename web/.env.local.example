# VoiceGPT Frontend Configuration
# Copy this file to .env.local and fill in your Firebase credentials

# Firebase Configuration
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Chat API Configuration
# IMPORTANT: This must point to the deployed Cloud Run service
# Local development should ALWAYS use the production endpoint
# Do NOT use localhost fallback - this ensures we test against real infrastructure
VITE_CHAT_API_URL=https://your-cloud-run-domain.run.app/chat

# Optional: For local testing with emulator (not recommended for production testing)
# VITE_CHAT_API_URL=http://localhost:8080/chat

